{
  "name": "healthcare-readmission-project",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "start": "concurrently \"d:\\healthcare-readmission-project\\venv\\Scripts\\python.exe -m uvicorn backend.main:app --reload\" \"cd frontend && npm start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
